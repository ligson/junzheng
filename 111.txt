　我该如何决定使用哪种整数类型？
　为什么不精确定义标准类型的大小？
　因为C语言没有精确定义类型的大小，所以我一般都用typedef定义int16和int32。然后根据实际的机器环境把它们定义为int、short、long等类型。这样看来，所有的问题都解决了，是吗？
　新的64位机上的64位类型是什么样的？
　这样的声明有什么问题？char*p1,p2;我在使用p2的时候报错了。
　我想声明一个指针，并为它分配一些空间，但却不行。这样的代码有什么问题？char*p;*p=malloc(10);
　怎样声明和定义全局变量和函数最好？
　如何在C中实现不透明(抽象)数据类型？
　如何生成“半全局变量”，就是那种只能被部分源文件中的部分函数访问的变量？
　同一个静态(static)函数或变量的所有声明都必需包含static存储类型吗？
　extern在函数声明中是什么意思？
　关键字auto到底有什么用途？
　对于用户定义类型，typedef和#define有什么区别？
　我似乎不能成功定义一个链表。我试过typedefstruct{char*item;NODEPTRnext;}*NODEPTR;但是编译器报了错误信息。难道在C语言中结构不能包含指向自己的指针吗？
　如何定义一对相互引用的结构？
　Struct{　}x1;和typedefstruct{　}x2;这两个声明有什么区别？
　“typedefint(*funcptr)();”是什么意思？
　我有这样一组声明：typedefchar*charp;constcharpp;为什么是p而不是它指向的字符为const?
　为什么不能像下面这样在初始式和数组维度值中使用const值？constintn=5;inta[n];
　constchar*p、charconst*p和char*constp有什么区别？
　怎样建立和理解非常复杂的声明？例如定义一个包含N个指向返回指向字符的指针的函数的指针的数组？
　如何声明返回指向同类型函数的指针的函数?我在设计一个状态机，用函数表示每种状态，每个函数都会返回一个指向下一个状态的函数的指针。可我找不到任何方法来声明这样的函数——感觉我需要一个返回指针的函数，返回的指针指向的又是返回指针的函数……，如此往复，以至无穷。
　能否声明和传入数组大小一致的局部数组，或者由其他参数指定大小的参数数组？
　我在一个文件中定义了一个extern数组，然后在另一个文件中使用，为什么sizeof取不到数组的大小？
　函数只定义了一次，调用了一次，但编译器提示非法重声明了。
　main的正确定义是什么？voidmain正确吗？
　我的编译器总在报函数原型不匹配的错误，可我觉得没什么问题。这是为什么？
　文件中的第一个声明就报出奇怪的语法错误，可我看没什么问题。这是为什么？
　为什么我的编译器不允许我定义大数组，如doublearray[][]?
如何判断哪些标识符可以使用，哪些被保留了？
　对于没有显式初始化的变量的初始值可以作怎样的假定？如果一个全局变量初始值为“零”，它可否作为空指针或浮点零？
　下面的代码为什么不能编译?intf(){chara[]="Hello,world!";}
　下面的初始化有什么问题？编译器提示“invalidinitializers”或其他信息。char*p=malloc(10);
　chara[]="stringliteral";和char*p="stringliteral";初始化有什么区别？当我向p[i]赋值的时候，我的程序崩溃了。
　chara{[3]}="abc";是否合法？
　我总算弄清楚函数指针的声明方法了，但怎样才能初始化呢？
　能够初始化联合吗？
　structx1{　};和typedefstruct{　}x2;有什么不同？
　这样的代码为什么不对？structx{　};xthestruct;
　结构可以包含指向自己的指针吗？
　在C语言中用什么方法实现抽象数据类型最好?
　在C语言中是否有模拟继承等面向对象程序设计特性的好方法？
　为什么声明externf(structx*p);给我报了一个晦涩难懂的警告信息？
　我遇到这样声明结构的代码：structname{intnamelen;charnamestr[1];};然后又使用一些内存分配技巧使namestr数组用起来好像有多个元素，namelen记录了元素个数。它是怎样工作的？这样是合法的和可移植的吗？
　我听说结构可以赋给变量也可以对函数传入和传出。为什么K&R1却明确说明不能这样做？
　为什么不能用内建的==和!=操作符比较结构？
结构传递和返回是如何实现的？
　如何向接受结构参数的函数传入常量值？怎样创建无名的中间的常量结构值？
　怎样从/向数据文件读/写结构？
　为什么我的编译器在结构中留下了空洞？这导致空间浪费而且无法与外部数据文件进行“二进制”读写。能否关掉填充，或者控制结构域的对齐方式？
　为什么sizeof返回的值大于结构大小的期望值，是不是尾部有填充？
　如何确定域在结构中的字节偏移量？
　怎样在运行时用名字访问结构中的域？
　C语言中有和Pascal的with等价的语句吗？
　既然数组名可以用作数组的基地址，为什么对结构不能这样？
　程序运行正确，但退出时却“coredump”(核心转储)了，怎么回事？
　结构和联合有什么区别？
　有办法初始化联合吗？
　有没有一种自动方法来跟踪联合的哪个域在使用？
　枚举和一组预处理的#define有什么不同？
　枚举可移植吗？
　有什么显示枚举值符号的容易方法吗？
　一些结构声明中的这些冒号和数字是什么意思？
　为什么人们那么喜欢用显式的掩码和位操作而不直接声明位域？
　为什么这样的代码不行？a[i]=i++;
　使用我的编译器，下面的代码inti=7;printf("%d\n",i++*i++);打印出49。不管按什么顺序计算，难道不该是56吗？
　对于代码inti=3;i=i++;不同编译器给出不同的i值，有的为3，有的为4，哪个是正确的？
　有这样一个巧妙的表达式：a^=b^=a^=b;它不需要临时变量就可以交换a和b的值。
　可否用显式括号来强制执行我所需要的计算顺序并控制相关的副作用？就算括号不行，操作符优先级是否能够控制计算顺序呢？
　可是&&和||操作符呢？我看到过类似while((c=getchar())!=EOF&&c!='\n')的代码……
　是否可以安全地认为，一旦&&和||左边的表达式已经决定了整个表达式的结果，则右边的表达式不会被求值？
　为什么表达式printf("%d%d",f1(),f2());先调用了f2？我觉得逗号表达式应该确保从左到右的求值顺序。
　怎样才能理解复杂表达式并避免写出未定义的表达式？“序列点”是什么？
在a[i]=i++;中，如果不关心a[]的哪一个分量会被写入，这段代码就没有问题，i也的确会增加1，对吗？
　人们总是说i=i++的行为是未定义的。可我刚刚在一个ANSI编译器上尝试过，其结果正如我所期望的。
　我不想学习那些复杂的规则，怎样才能避免这些未定义的求值顺序问题呢？
　++i和i++有什么区别？
　如果我不使用表达式的值，那我应该用i++还是++i来做自增呢？
　我要检查一个数是不是在另外两个数之间，为什么if(abc)不行？
　为什么如下的代码不对？inta=,b=;longintc=a*b;
　为什么下面的代码总是给出0？doubledegC,degF;degC=/9*(degF-32);
　需要根据条件把一个复杂的表达式赋给两个变量中的一个。可以用下面这样的代码吗？((condition)?a:b)=complicated_expression;
　我有些代码包含这样的表达式。a?b=c:d有些编译器可以接受，有些却不能。为什么？
　“semanticsof‘’changeinANSIC”的警告是什么意思？
　“无符号保护”和“值保护”规则的区别在哪里？
　指针到底有什么好处？
　我想声明一个指针并为它分配一些空间，但却不行。这些代码有什么问题呢？char*p;*p=malloc(10);
　*p++自增p还是p所指向的变量？
　我用指针操作int数组的时候遇到了麻烦。
　我有一个char*型指针碰巧指向一些int型变量，我想跳过它们。为什么((int*)p)++;这样的代码不行？
　为什么不能对void*指针进行算术操作？
　我有些解析外部结构的代码，但是它却崩溃了，显示出了“unalignedaccess”(未对齐的访问)的信息。这是什么意思？
　我有个函数，它应该接受并初始化一个指针：voidf(int*ip){staticintdummy=5;ip=&dummy;}但是当我如下调用时：int*ip;f(ip);调用者的指针没有任何变化。
　能否用void**通用指针作为参数，使函数模拟按引用传递参数?
　我有一个函数externintf(int*);，它接受指向int型的指针。我怎样用引用方式传入一个常数？调用f(&5);似乎不行。
　C语言可以“按引用传参”吗？
　我看到了用指针调用函数的不同语法形式。到底怎么回事？
　通用指针类型是什么？当我把函数指针赋向void*类型的时候，编译通不过。
　怎样在整型和指针之间进行转换？能否暂时把整数放入指针变量中，或者相反？
　我怎样把一个int变量转换为char*型？我试了类型转换，但是不行。
　臭名昭著的空指针到底是什么？
　怎样在程序里获得一个空指针？
　用缩写的指针比较“if(p)”检查空指针是否有效？如果空指针的内部表达不是0会怎样？
　NULL是什么，它是怎么定义的？
　在使用非零位模式作为空指针的内部表示的机器上，NULL是如何定义的？
　如果NULL定义成#defineNULL((char*)0)，不就可以向函数传入不加转换的NULL了吗？
　我的编译器提供的头文件中定义的NULL为0L。为什么？
　NULL可以合法地用作函数指针吗？
　如果NULL和0作为空指针常量是等价的，那我到底该用哪一个呢？
但是如果NULL的值改变了，比如在使用非零内部空指针的机器上，用NULL(而不是0)
　我曾经使用过一个编译器，不使用NULL就不能编译。
　我用预处理宏#defineNullptr(type)(type*)0帮助创建正确类型的空指针。
　这有点奇怪：NULL可以确保是0，但空(null)指针却不一定？
　为什么有那么多关于空指针的疑惑？为什么这些问题如此频繁地出现？
　有没有什么简单点儿的办法理解所有这些与空指针有关的东西呢？
　考虑到有关空指针的所有这些困惑，要求它们的内部表示都必须为0不是更简单吗？
　说真的，真有机器用非零空指针吗，或者不同类型用不同的表示？
　运行时的整数值0转换为指针以后一定是空指针吗？
　如何访问位于机器地址0处的中断向量？如果我将指针值设为0，编译器可能会自动将它转换为非零的空指针内部表示。
运行时的“nullpointerassignment”错误是什么意思？应该怎样捕捉它？
　我在一个源文件中定义了chara[6]，在另一个源文件中声明了externchar*a。为什么不行？
　可是我听说chara[]和char*a是等价的。是这样的吗？
　那么，在C语言中“指针和数组等价”到底是什么意思？
　既然它们这么不同，那为什么作为函数形参的数组和指针声明可以互换呢？
　为什么不能这样向数组赋值？externchar*getpass();charstr[10];str=getpass("Enterpassword:");
　既然不能向数组赋值，那这段代码为什么可以呢？intf(charstr[]){if(str[0]=='\0')str="none";…}
　如果你不能给它赋值，那么数组如何能成为左值呢？
　现实地讲，数组和指针的区别是什么？
　有人跟我讲，数组不过是常指针。这样讲准确吗？
　我还是很困惑。到底指针是一种数组，还是数组是一种指针？
　我看到一些“搞笑”的代码，包含5["abcdef"]这样的“表达式”。这为什么是合法的C语言表达式呢？
　既然数组引用会退化为指针，如果array是数组，那么array和&array又有什么区别呢？
　如何声明一个数组的指针？
　如何在运行时设定数组的大小？怎样才能避免固定大小的数组？
　我如何声明大小和传入的数组一样的局部数组？
　如何动态分配多维数组？
　有个很好的窍门，如果我这样写：intrealarray[10];int*array=&realarray[-1];我就可以把“array”当作下标从1　开始的数组。
　当我向一个接受指针的指针的函数传入二维数组的时候，编译器报错了。
　我怎样编写接受编译时宽度未知的二维数组的函数？
　我怎样在函数参数传递时混用静态和动态多维数组？
　当数组是函数的参数时，为什么sizeof不能正确报告数组的大小？
　如何在一个文件中判断声明为extern的数组的大小(例如，数组定义和大小在另一个文件中)？sizeof操作符似乎不行。
　sizeof返回的大小是以字节计算的，怎样才能判断数组中有多少个元素呢？
　为什么这段代码不行？char*answer;printf("Typesomething:\n");gets(answer);printf("Youtyped\"%s\"\n",answer);
　我的strcat()不行。我试了下面的代码：char*s1="Hello,";char*s2="world!";char*s3=strcat(s1,s2);但是我得到了奇怪的结果。
　但是strcat的文档说它接受两个char*型参数。我怎么知道(空间)分配的事情呢？
　我刚才试了这样的代码：char*p;strcpy(p,"abc");它运行正常。怎么回事？为什么它没有出错？
　一个指针变量分配多少内存？
　我使用fgets将文件的所有行读入一个数组，为什么读入的每一行都是最后一行的内容呢？
　我有个函数，本该返回一个字符串，但当它返回调用者的时候，返回的字符串却是垃圾信息。
　那么返回字符串或其他聚集的正确方法是什么呢？
　为什么在调用malloc()时报出了“waring:assignmentofpointerfromintegerlacksacast”？
为什么有些代码小心翼翼地把malloc返回的值转换为分配的指针类型？
　在调用malloc()的时候，错误“不能把void*转换为int*”是什么意思？
　我看到下面这样的代码：char*p=malloc(strlen(s)+1);strcpy(p,s);难道不应该是malloc((strlen(s)+1)*sizeof(char))吗？
　我为malloc写了一个小小的封装函数。它为什么不行？
　我想声明一个指针并向它分配一些内存，但是不行。这样的代码有什么问题？char*p;*p=malloc(10);
　我如何动态分配数组？
　怎样判断还有多少内存？
　malloc(0)是返回空指针还是指向0个字节的指针？
　我听说有的操作系统在程序使用的时候才真正分配malloc申请的内存。这合法吗？
　为什么malloc返回了离谱的指针值？我的确读过问题，而且也在调用之前包含了externvoid*malloc();声明。
　我用一行这样的代码分配一个巨大的数组，用于数值运算：double*array=malloc(　*　*sizeof(double));malloc()并没有返回空指针，但是程序运行得有些奇怪，好像改写了某些内存，或者malloc()并没有分配我申请的那么多内存。为什么？
　我的PC机有8兆内存。为什么我只能分配K左右的内存？
　我的应用程序非常依赖数据结构的节点的动态分配，而malloc/free的代价成了瓶颈。我该怎么做？
　我的程序总是崩溃，显然发生在malloc内部的某个地方。但是我看不出哪里有问题。是malloc有bug吗？
　动态分配的内存一旦释放之后就不能再使用，是吧？
　为什么在调用free()之后指针没有变空？使用(赋值、比较)释放之后的指针有多么不安全？
　当我调用malloc()为一个函数的局部指针分配内存时，我还需要用free()显式地释放吗？
　我在分配一些结构，它们包含指向其他动态分配的对象的指针。我在释放结构的时候，还需要释放每一个下级指针吗？
　我必须在程序退出之前释放分配的所有内存吗？
　我有个程序分配了大量的内存，然后又释放了。但是从操作系统看，内存的占用率却并没有变回去。
　free()怎么知道有多少字节需要释放？
　那么我能否查询malloc包，以查明可分配的最大块是多大？
　为什么sizeof不能告诉我它所指的内存块的大小？
　(像问题中那样)动态分配数组之后，还能改变它的大小吗？
　向realloc()的第一个参数传入空指针合法吗？你为什么要这样做？
　calloc()和malloc()有什么区别？应该用哪一个？利用calloc的零填充功能安全吗？free()可以释放calloc()分配的内存吗，还是需要一个cfree()？
　alloca是什么？为什么不提倡使用它？
　为什么strcat(string,'!');不行？
　我想检查一个字符串是否跟某个值匹配。为什么这样不行？if(string=="value")
　如果我可以写chara[]="Hello,world!";那为什么不能写chara[14];a="Hello,world!";
　为什么我的strcat不行？我试了char*s1="Hello,";char*s2="world!";char*s3　=strcat(s1,s2);可得到的结果很奇怪。
　chara[]="stringliteral";和char*p="stringliteral";初始化有什么区别？当我对p[i]赋值的时候，程序崩溃了。
　我怎么得到与字符相对应的数字(即ASCII或其他字符集下的)值？反过来又该怎么做？
　C语言有类似其他语言的"substr"(提取子串)这样的函数吗？
　我将用户键入的字符串读入数组，然后再显示出来。当用户键入\n这样的序列时，为什么不能正确处理呢？
　我注意到sizeof('a')是2而不是1(即不是sizeof(char))，是不是我的编译器有问题？
　我正开始考虑多语言字符集的问题。是否有必要担心sizeof(char)会被定义为2，以便表达16位的字符集呢？
　C语言中布尔值该用什么类型？为什么它不是一个标准类型？我应该用#define或enum定义真值和假值吗？
　既然在C语言中所有的非零值都被看作“真”，那是不是把TRUE定义为1很危险？如果某个内建的函数或关系操作符“返回”不是1的其他值怎么办？
　当p是指针时，if(p)是合法的条件表达式吗？
　我该使用像TRUE和FALSE这样的符号名称还是直接用来作布尔常量？
　我准备使用的一个第三方头文件定义了自己的TRUE和FALSE，它们跟我已经开发的部分不兼容。我该怎么办？
　我想定义一些函数式的宏，例如：#definesquare(x)x*x但它们并不总是正确的。为什么？
　这里有一些的预处理宏，使用它们，我可以写出更像Pascal的C代码。你觉得怎么样？
　怎么写一个交换两个值的通用宏？
　书写多语句宏的最好方法是什么？
　用typdef和预处理宏生成用户定义类型有什么区别？
　我第一次把一个程序分成多个源文件，我不知道该把什么放到.c文件，把什么放到.h文件。(“.h”到底是什么意思？)
　可以在一个头文件中包含另一头文件吗？
　完整的头文件搜索规则是怎样的？
　我在文件的第一个声明就遇到奇怪的语法错误，但是看上去没什么问题。
　我使用了来自两个不同的第三方库的头文件，它们都定义了相同的宏，如TRUE、FALSE、Min()和Max()等，但是它们的定义相互冲突，而且跟我在自己的头文件中的定义也有冲突。我该怎么办？
　我在编译一个程序，看起来我好像缺少需要的一个或多个头文件。谁能发给我一份？
　怎样构造比较字符串的#if预处理表达式？
　sizeof操作符可以用在#if预处理指令中吗？
　我可以像这样在#define行里使用#ifdef来定义两个不同的东西吗？
　对typedef的类型定义有没有类似#ifdef的东西？
　我如何用#if表达式来判断机器是高字节在前还是低字节在前？
　为什么在我用#ifdef关掉的代码行中报出了奇怪的语法错误？
　我拿到了一些代码，里边有太多的#ifdef。我不想使用预处理器把所有的#include和#ifdef都扩展开，有什么办法只保留一种条件的代码呢？
　如何列出所有的预定义宏？
　我有些旧代码，试图用这样的宏来构造标识符：#definePaste(a,b)a/**/b但是不行了。为什么？
　我有一个旧宏：#defineCTRL(c)('c'&)不能用了。为什么？
　为什么宏#defineTRACE(n)printf("TRACE:\%d\n",n)报出警告“macroreplacementwithinastringliteral”？它似乎把TRACE(count);扩展成了printf("TRACE:\%d\count",count);
　如何在宏扩展的字符串字面量中使用宏参数？
　我想用ANSI的“字符串化”预处理操作符#将符号常量的值放入消息中，但它总是对宏名称而不是它的值进行字符串化。这是什么原因？
　我想用预处理器做某件事情，但却不知道如何下手。
　怎样写可变参数宏？如何用预处理器“关掉”具有可变参数的函数调用？
　如何在通用的调试宏中包含__FILE__和__LINE__宏？
　什么是“ANSIC标准”？
　如何得到一份标准的副本？
　我在哪里可以找到标准的更新？
　为什么我的ANSI编译器对用float声明的参数会警告类型不匹配？
　能否混用旧式的和新型的函数语法？
　为什么下述声明报出了一个奇怪的警告信息“StructXdeclaredinsideparameterlist”？externintf(structx*p);
　有个问题一直困扰着我，它是由这一行printf("%d",n);导致的，因为n是个longint型。难道ANSI的函数原型不能检查这种函数的参数不匹配问题吗？
　我听说必须在调用printf之前包含stdio.h。为什么？
　为什么不能在初始化和数组维度中使用const值？例如constintn=5;inta[n];
“constchar*p”、“charconst*p”和“char*constp”有何区别？
　为什么不能向接受constchar**的函数传入char**？
　我这样声明：typedefchar*charp;constcharpp;为什么是p而不是它所指向的字符为const？
　能否通过将main声明为void来关掉“main没有返回值”的警告？
　main()的第3个参数envp是怎么回事？
　我觉得把main()声明为void也不会失败，因为我调用了exit()而不是return，况且我的操作系统也忽略了程序的退出/返回状态。
　那么到底会出什么问题？真的有什么系统不支持voidmain()吗？
　为什么以前流行的那些C语言书总是使用voidmain()？
　在main()中调用exit(status)和返回同样的status真的等价吗？
　我试图用ANSI“字符串化”预处理操作符'#'向信息中插入符号常量的值，但它字符串化的总是宏的名字而不是它的值。为什么？
　警告信息“warning:macroreplacementwithinastringliteral”是什么意思？
　为什么在我用#ifdef去掉的代码里出现了奇怪的语法错误？
　#pragma是什么，有什么用？
　“#pragmaonce”什么意思？我在一些头文件中看到了它。
　chara[3]="abc";合法吗？它是什么意思？
　既然对数组的引用会退化为指针，那么，如果array是数组，array和&array之间有什么区别呢？
　为什么我不能对void*指针进行算术运算？
　memcpy()和memmove()有什么区别？
　malloc(0)有什么用？返回一个空指针还是指向0字节的指针？
　为什么ANSI标准规定了外部标识符的长度和大小写限制？
　noalias是怎么回事？在它身上发生了什么？
　为什么我的编译器对最简单的测试程序都报出了一大堆的语法错误？对这段代码的第一行就报错了：main(intargc.char**argv){return0;}
　为什么有些ASNI/ISO标准库函数未定义？我明明使用的就是ANSI编译器。
　谁有可以在旧的C程序和ANSIC之间相互转换的工具，或者自动生成原型的工具？
　为什么声称兼容ANSI的编译器不能编译这些代码？我知道这些代码是ANSI的，因为gcc可以编译。
　人们好像有些在意实现定义的(implementation-defined)、不确定的(unspecified)和未定义的(undefined)行为的区别。它们的区别到底在哪里？
　一个程序“合法(legal)”、“有效(valid)”或“符合标准的”(conforming)到底是什么意思？
　我很吃惊，ANSI标准竟然有那么多未定义的东西。标准的唯一任务不就是让这些东西标准化吗？
　有人说i=i++的行为是未定义的，但是我刚在一个兼容ANSI的编译器上测试，得到了我希望的结果。它真的是未定义的吗？
　这样的代码有什么问题？charc;while((c=getchar())!=EOF)
　我有个读取直到EOF的简单程序，但是我如何才能在键盘上输入那个“\EOF”呢？我看stdio.h中定义的EOF是-1，是不是说我该输入-1？
　为什么这些代码把最后一行复制了两遍？while(!feof(infp)){fgets(buf,MAXLINE,infp);fputs(buf,outfp);}
　我用fgets将文件的每行内容读入指针数组。为什么结果所有的行都是最后一行的内容呢？
　我的程序的屏幕提示和中间输出有时没有在屏幕上显示，尤其是当我用管道通过另一个程序输出的时候。为什么？
　我怎样才能不等待回车键而一次输入一个字符？
　如何在printf的格式串中输出一个'%'字符？我试过\%，但是不行。
　为什么这么写不对？longintn=;printf("%d\n",n);
　有人告诉我不能在printf中使用%lf。为什么printf()用%f输出double型，而scanf却用%lf呢？
　对于size_t那样的类型定义，当我不知道它到底是long还是其他类型的时候，我应该使用什么样的printf格式呢？
　如何用printf实现可变的域宽度？就是说，我想在运行时确定宽度而不是使用%8d？
　如何输出在千位上用逗号隔开的数字？货币格式的数字呢？
　为什么scanf("%d",i)调用不行？
　为什么chars[30];scamf("%s",s);不用&也可以？我原以为传给scanf的每个变量都要带&。
　为什么这些代码不行？doubled;scanf("%f",&d);
　为什么这段代码不行？shortints;scanf("%d",&s);
　怎样在scanf格式串中指定可变的宽度？
　怎样从特定格式的数据文件中读取数据？怎样读入10个float而不用使用包含10次%f的奇怪格式？如何将一行的任意多个域读入一个数组中？
　我像这样用"%d\n"调用scanf从键盘读取数字：intn;scanf("%d\n",&n);printf("youtyped%d\n",n);好像要多输入一行才返回。为什么？
　我用scanf和%d读取一个数字，然后再用gets()读取字符串，但是编译器好像跳过了gets()调用！
　我发现如果坚持检查返回值以确保用户输入的是我期待的数值，则scanf的使用会安全很多。但有的时候好像会陷入无限循环。为什么？
　为什么大家都说不要使用scanf？那我该用什么来代替呢？
　我怎样才知道对于任意的sprintf调用需要多大的目标缓冲区？怎样才能避免sprintf目标缓冲区溢出？
　sprintf的返回值是什么？是int还是char*？
　为什么大家都说不要使用gets？
　我觉得我应该在一长串的printf调用之后检查errno，以确定是否有失败的调用。为什么当我将输出重定向到文件的时候会输出奇怪的“printffailed:Notatypewriter”信息？
　fgetops/fsetops和ftell/fseek之间有什么区别？fgetops和fsetops到底有什么用处？
　如何清除用户的多余输入，以防止在下一个提示符下读入？用fflush(stdin)可以吗？
　我写了一个函数用来打开文件：myfopen(char*filename,FILE*fp){fp=fopen(filename,"r");}可我这样调用的时候：FILE*infp;myfopen("filename.dat",infp);，infp指针并没有正确设置。为什么？
　连一个最简单的fopen调用都不成功！这个调用有什么问题？FILE*fp=fopen(filename,'r');
　为什么我不能用完整路径名打开一个文件？这个调用总是失败：fopen("c:\newdir\file.dat","r");
　我想用fopen模式"r+"打开一个文件，读出一个字符串，修改之后再写入，从而就地更新一个文件。可是这样不行。为什么？
　如何在文件中间插入或删除一行(一条记录)？
　怎样从打开的流中恢复文件名？
　怎样在程序里把stdin或stdout重定向到文件？
　一旦使用freopen之后，怎样才能恢复原来的stdout(或stdin)？
　如何判断标准输入或输出是否经过了重定向，即是否在命令行上使用了“”或“”？
　我想写个像"more"那样的程序。怎样才能在stdin被重定向之后再回到交互键盘？
　怎样同时向两个地方输出，如同时输出到屏幕和文件？
　我希望按字节在内存和文件之间直接读写数字，而不像fprintf和fscanf进行格式化。我该怎么办？
　怎样正确地读取二进制文件？有时看到a和d容易混淆，而且如果数据中包含a的话，我好像会提前遇到EOF。
　我在写一个二进制文件的“过滤器”，但是stdin和stdout却被作为文本流打开了。怎样才能把它们的模式改为二进制？
　文本和二进制输入输出有什么区别？
　如何在数据文件中读写结构？
　怎样编写符合旧的二进制数据格式的代码？
　怎样把数字转为字符串(与atoi相反)？有itoa函数吗？
　为什么strncpy不能总在目标串放上终止符'\0'？
　C语言有类似于其他语言中的“substr”(取出子串)的例程吗？
　怎样把一个字符串中所有字符转换成大写或小写？
　为什么有些版本的toupper对大写字符会有奇怪的反应？为什么有的代码在调用toupper前先调用islower？
　怎样将字符串分割成用空白分隔的字段？怎样实现类似main处理argc和argv的过程？
　哪里可以找到处理正则表达式或通配符匹配的代码？
　我想用strcmp作为比较函数，调用qsort对一个字符串数组排序，但是不行。为什么？
　我想用qsort()对一个结构数组排序。我的比较函数接受结构指针，但是编译器认为这个函数不是qsort需要的类型。我要怎样转换这个函数指针才能避免这样的警告？
　怎样对一个链表排序？
　怎样对大于内存容量的数据排序？
　怎样在C程序中取得当前日期或时间？
　我知道库函数localtime可以把time_t转换成结构structtm，而ctime可以把time_t转换成为可打印的字符串。怎样才能进行反向操作，把structtm或一个字符串转换成time_t？
　怎样在日期上加n天？怎样取得两个日期的时间间隔？
　怎么生成一个随机数？
　怎样获得某一范围内的随机整数？
　每次执行程序，rand都返回相同的数字序列。为什么？
　我需要随机的真/假值，所以我就直接用rand()%2，可是我得到交替的,,0…。为什么？　
　如何获取根本不重复的随机数？
　怎样产生正态分布或高斯分布的随机数？
　我在移植一个程序，里边调用了一个函数drand48　，而我的库又没有这个。这是个什么函数？
　exit(status)是否真的跟从main函数返回status等价？
　memcpy和memmove有什么区别？
　我想移植这个旧程序。为什么报出这些“undefinedexternal”错误：index?、rindex?、bcopy?、bcmp?、bzero?？
　我不断得到库函数未定义错误，但是我已经包含了所有用到的头文件了。
　虽然我在连接时明确地指定了正确的函数库，我还是得到库函数未定义错误。
　一个最简单的程序，不过在一个窗口里打印出“Hello,World”，为什么会编译出巨大的可执行代码(数百K)？我该少包含一些头文件吗？
　连接器报告_end未定义代表什么意思？
　我的编译器提示printf未定义！这怎么可能？
　一个float变量赋值为时，为什么printf输出的值为？
　我想计算一些平方根，我把程序简化成这样：main(){printf("%f\h",sqrt(.));可得到的结果却是疯狂的数字。为什么？
　我想做一些简单的三角函数运算，也包含了math.h，但连接器总是提示sin、cos这样的函数未定义。为什么？
　我的浮点数计算程序表现得很奇怪，在不同的机器上给出了不同的结果。为什么？
　有什么好的方法来检查浮点数在“足够接近”情况下的相等？
　怎样取整？
　为什么C语言不提供乘幂的操作符？
　为什么我机器上的math.h没有预定义常量M_PI？
　怎样将变量置为IEEENaN(“NotaNumber”)或检测变量是否为NaN及其他特殊值？
　如何简洁地处理浮点异常？
　在C语言中如何很好地实现复数？
　我要寻找一些实现以下功能的程序源代码：快速傅立叶变换(FFT)、矩阵算术(乘法、求逆等函数)、复数算术。
　TurboC的程序崩溃，显示错误为“floatingpointformatsnotlinked”(浮点格式未连接)。我还缺点儿什么呢？
　为什么调用printf前必须要包含stdio.h？
　为什么%f可以在printf参数中同时表示float和double？它们难道不是不同类型吗？
　我遇到了一个令人十分受挫的问题，后来发现是这行代码造成的：printf("%d",n);原来n是longint型。难道ANSI的函数原型不就是用来防止这类的参数类型不匹配吗？
　怎样写一个接受可变参数的函数？
　怎样写一个函数，像printf那样接受一个格式串和可变参数，然后再把参数传给printf去完成大部分工作？
　怎样写类似scanf的函数，再把参数传给scanf去完成大部分工作？
　我用的是ANSI前的编译器，没有stdarg.h文件。我该怎么办？
　怎样知道实际上有多少个参数传入函数？
　为什么编译器不允许我定义一个没有固定参数项的可变参数函数？
　我有个接受float型的变参函数，为什么va_arg(argp,float)却不行？
　为什么va_arg不能得到类型为函数指针的参数？
　怎样实现一个可变参数函数，它把参数再传给另一个可变参数函数？
　怎样调用一个在运行时才构建参数列表的函数？
　为什么这个循环只执行了一次？for(i=start;iend;i++);{printf("%d\n",i);}
　遇到不可理解的不合理语法错误，似乎大段的程序没有编译。
　为什么过程调用不起作用？编译器似乎直接跳过去了。
　程序在执行之前就崩溃了！(用调试器单步跟踪，在main函数的第一个语句之前就死了。)为什么？　　程序执行正确，但退出时在main函数的最后一个语句之后崩溃了。为什么会这样？
　程序在一台机器上运行完美，但在另一台上却得到怪异的结果。更奇怪的是，增加或去除调试的打印语句，就改变了症状……
　为什么下面的代码会崩溃？char*p="hello,world!";p[0]='H';
　我有些代码是用来解析外部结构的，但它却崩溃了，报了“unalignedaccess”(未对齐的访问)错误。这是什么意思？
　“Segmentationviolation”、“Buserror”和“Generalprotectionfault”是什么意思？
　什么是C最好的代码布局风格？
　如何在源文件中合理分配函数？
　用if(!strcmp(s1,s2))比较两个字符串是否相等是个好风格吗？
　为什么有的人用if(0==x)而不是if(x==0)？
　为什么有些代码在每次调用printf前增加了类型转换(void)？
　既然NULL和0都是空指针常量，我到底该用哪一个？
　是该用TRUE和FALSE这样的符号名称还是直接用来作布尔常量？
　什么是“匈牙利表示法”(HungarianNotation)？是否值得一试？
　哪里可以找到“IndianHillStyleGuide”及其他编码标准？
　有人说goto是邪恶的，永远都不该用它。这是否太极端了？
　人们总是说良好的风格很重要，但当他们使用良好的风格写出清晰易读的程序后，又发现程序的效率似乎降低了。既然效率那么重要，是否可以为了效率牺牲一些风格和可读性呢？
　能否列一个常用工具列表？
　怎样捕获棘手的malloc问题？
　有什么免费或便宜的编译器可以使用？
　刚刚输入完一个程序，但它表现得很奇怪。你能发现有什么错误的地方吗？
　如何关掉lint对每个malloc调用报出的“warning:possiblepointeralignmentproblem”警告消息？
　哪里可以找到兼容ANSI的lint？
　难道ANSI函数原型说明没有使lint过时吗？
　网上有哪些C语言的教程或其他资源？
　哪里可以找到好的源代码实例，以供研究和学习？
　有什么好的学习C语言的书？有哪些高级的书和参考？
　哪里能找到K&R的练习答案？
　哪里能找到NumericalRecipesinC、Plauger的TheStandardCLibrary或Kernighan和Pike的TheUNIXProgrammingEnviroment等书里的源码？
　哪里可以找到标准C函数库的源代码？
　是否有一个在线的C参考指南？
　我需要分析和评估表达式的代码。从哪里可以找到？
　哪里可以找到C的BNF或YACC语法？
　谁有C编译器的测试套件？
　哪里有一些有用的源代码片段和例子的收集？
　我需要执行多精度算术的代码。
　在哪里和怎样取得这些可自由发布的程序？
　怎样从键盘直接读入字符而不用等回车键？怎样防止字符输入时的回显？
　怎样知道有未读的字符(如果有，有多少)？另外，如何在没有字符的时候不阻塞读入？
　怎样显示一个在原地更新自己的百分比或“旋转棒”的进度指示器？
　怎样清屏？怎样反色输出？怎样把光标移动到指定的x,y位置？
　怎样读入方向键、功能键？
　怎样读入鼠标输入？
　怎样做串口(“comm”)的输入输出？
　怎样直接输出到打印机？
　怎样发送转义字符序列控制终端或其他设备？
　怎样做图形？
　怎样显示GIF和JPEG图像？
　怎样检验一个文件是否存在？如果请求的输入文件不存在，我希望向用户提出警告。
　怎样在读入文件前，知道文件大小？
　怎样得到文件的修改日期和时间？
　怎样原地缩短一个文件而不用清除或重写？
　怎样在文件中插入或删除一行(或一条记录)？
　怎样从一个打开的流或文件描述符得到文件名？
　怎样删除一个文件？
　怎样复制文件？
　为什么用了详尽的路径还不能打开文件？下面的代码会返回错误。Fopen("c:\newdir\file.dat","r")
　fopen不让我打开文件"$HOME/.profile"和"~~/.myrcfile"。
　怎样制止MS-DOS下令人恐怖的“Abort，Retry，Ignore?”信息？
　遇到“Toomanyopenfiles(打开文件太多)”的错误，怎样增加同时打开文件的允许数目？
　如何得到磁盘的可用空间大小？
　怎样在C语言中读入目录？
　如何创建目录？如何删除目录(及其内容)？
　怎样找出系统还有多少内存可用？
　怎样分配大于64K的数组或结构？
　错误信息“DGROUPdataallocationexceeds64K(DGROUP数据分配内存超过64K)”什么意思？我应该怎么做？我以为使用了大内存模型，就可以使用大于64K的数据！
　怎样访问位于某特定地址的内存(内存映射的设备或图形显示内存)？
　如何访问机器地址0处的中断向量？如果将指针设为0，编译器可能把它转成一个非零的内部空指针值。
　怎样在一个C程序中调用另一个程序(独立可执行的程序或系统命令)？
　如果运行时才知道要执行的命令的参数(文件名等)，应该如何调用system？
　在MS-DOS上如何得到system返回的准确错误状态？
　怎样调用另一个程序或命令，然后获取它的输出？
　怎样才能发现程序自己的执行文件的全路径？
　怎样找出和执行文件在同一目录的配置文件？
　进程如何改变它的调用者的环境变量？
　如何打开命令行给出的文件并解析选项？
　exit(status)是否真的和从main函数返回同样的status等价？
　怎样读入一个对象文件并跳跃到其中的函数？
　怎样以小于1秒的精度延时或计算用户响应时间？
　怎样捕获或忽略control-C这样的键盘中断？
　怎样简洁地处理浮点异常？
　怎样使用socket？如何联网？如何写客户/服务器程序？
　怎样调用BIOS函数？如何写ISR？如何创建TSR？
　什么是“near”和“far”指针？
　我不能使用这些非标准、依赖系统的函数，程序需要兼容ANSI!
　为什么这些内容没有在C语言中进行标准化？任何现实程序都会用到这些东西。
　怎样从函数返回多个值？
　用什么数据结构存储文本行最好？我开始用固定大小的char型数组的数组，但是有很多局限。
　怎样打开命令行提到的文件并处理参数？
　如何正确地使用errno？
　怎样写数据文件，使之可以在不同字大小、字节顺序或浮点格式的机器上读入？
　怎样用char*指针指向的函数名调用函数？
　如何操作各个位？
　怎样实现位数组或集合？　
　怎样判断机器的字节顺序是高字节在前还是低字节在前？
　怎样调换字节？
　怎样将整数转换到二进制或十六进制？
　可以使用二进制常数(类似这样的东西)吗？printf有二进制的格式说明符吗？
　用什么方法计算整数中为1的位的个数最高效？
　怎样提高程序的效率？
　指针真的比数组快吗？函数调用会拖慢程序多少？++i比i=i+1快吗？
　用移位操作符替换乘法和除法是否有价值？
　人们说编译器优化得很好，我们不再需要为速度而写汇编了，但我的编译器连用移位代替i/=2都做不到。
　怎样不用临时变量而交换两个值？
　switch语句和if/else链哪个更高效？
　是否有根据字符串进行条件切换的方法？
　是否有使用非常量case行标的方法(如范围或任意的表达式)？
　return语句外层的括号是否真的可选择？
　为什么C语言的注释不能嵌套？怎样注释掉含有注释的代码？引号包含的字符串内的注释是否合法？
　为什么C语言的操作符不设计得更全面一些？好像还缺了一些^^、&&=和-=这样的操作符。
　C语言有循环移位操作符吗？
　C是个伟大的语言还是别的什么东西？哪个其他语言可以写出像a+++++b这样的代码？
　如果赋值操作符是:=，是不是就不容易意外地写出if(a=b)了？
　C语言有和Pascal的with等价的语句吗？
　为什么C语言没有嵌套函数？
　assert是什么？如何使用？
　怎样从C中调用FORTRAN(C++、BASIC、Pascal、Ada、LISP)的函数？反之如何？
　有什么程序可以将Pascal或FORTRAN(或LISP、Ada、awk、“老”C)程序转化为C程序？
　C++是C的超集吗？可以用C++编译器来编译C代码吗？
　我需要用到“近似”的strcmp例程，比较两个字符串的近似度，并不需要完全一样。有什么好办法？
　什么是散列法？
　如何生成正态或高斯分布的随机数？
　如何知道某个日期是星期几？
　(year%4==0)是否足以判断闰年？年是闰年吗？
　为什么tm结构中的tm_sec的范围是，暗示一分钟有62秒？
　一个难题：怎样写一个输出自己源代码的程序？
　什么是“达夫设备”(Duff’sDevice)？
　下届国际C语言混乱代码竞赛(InternationalObfuscatedCCodeContest，IOCCC)什么时候进行？哪里可以找到当前和以前的获胜代码？
　K&R1提到的关键字entry是什么？
　C的名字从何而来？
　“char”如何发音？
　“lvalue”和“rvalue”代表什么意思？